# STC Solutions AI Edge Server - API + UI + trial + background workers
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
aiosqlite>=0.19.0
pydantic>=2.0
python-multipart>=0.0.6
# Inference (shared)
onnxruntime>=1.16.0
numpy>=1.24,<2
Pillow>=10.0
# Optional: sync events to cloud
httpx>=0.26.0
# For live snapshot from RTSP (البث المباشر)
opencv-python-headless>=4.8
