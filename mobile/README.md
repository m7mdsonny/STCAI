# STC Solutions AI Mobile (Flutter)

OTP login, multi-site, events, snapshot/clip, acknowledge, escalate, remote siren, arm/disarm.  
See `docs/mobile/01-mobile-app-spec.md` and `docs/api/openapi-cloud.yaml`.

```bash
flutter pub get
flutter run
```

Set API base in code or env (e.g. `lib/api/client.dart` baseUrl).
